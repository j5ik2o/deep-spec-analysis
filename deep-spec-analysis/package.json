{
  "name": "deep-spec-analysis-plugin-dev",
  "private": true,
  "workspaces": [
    "src/*/*",
    "src/entries",
    "tests"
  ],
  "description": "Dev-only harness for the deep-spec-analysis AIDLC plugin: pinned solver backends for the conformance tests. Nothing here ships — plugin projections carry only the conventional content directories.",
  "scripts": {
    "check": "bun run check:biome && bun run lint:usecase-getters",
    "check:biome": "biome check --error-on-warnings .",
    "check:fix": "biome check --write --error-on-warnings .",
    "format": "biome format --write .",
    "lint": "biome lint --error-on-warnings . && bun run lint:usecase-getters",
    "lint:usecase-getters": "bun scripts/lint-usecase-getters.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.12",
    "@informalsystems/quint": "0.32.0",
    "@types/bun": "1.4.0",
    "typescript": "7.0.2",
    "z3-solver": "5.2.0"
  }
}
